sessions/
data/
dist/
release/
.dev/
node_modules/
src/accounts.json
src/accounts.enc.json
src/config.json
/.vscode
/.core/
scripts/runtime/
/diagnostics
note
accounts.dev.json
accounts.main.json
.DS_Store
.playwright-chromium-installed
bun.lock
logs/
.plugins-marketplace*.log
.updates/
.tools/
.stfolder/
Page/
plugins/*/dist/
plugins/*/build/
plugins/*/coverage/
plugins/*/.cache/
plugins/*/node_modules/
plugins/core-source/
plugins/.data/
plugins/*/.installed.json
plugins/.marketplace-seq
# Live catalog cache: re-synced from core-api on every bot start (see
# syncMarketplaceCatalog in src/core/PluginManager.ts). Never ship a snapshot —
# plugins/marketplace.example.json is the committed reference/template instead.
plugins/marketplace.json
plugins/marketplace.sig
core-source/
*.jsc.map
license-admin.config.local.js
plugins/core/license-admin.config.local.js
.desk-notices.json
*.bak
src/*.bak

# Secrets / credentials — never commit (this is how a sibling repo leaked its .env)
.env
.env.*
!.env.example
*.pem
# ...but the PUBLIC keys shipped under scripts/security/ MUST stay tracked (clients
# verify Core + the marketplace catalog against them). Re-include them explicitly.
!scripts/security/*.pem
!scripts/security/**/*.pem
_keys/
